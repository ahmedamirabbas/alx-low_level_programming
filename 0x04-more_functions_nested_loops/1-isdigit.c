#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: the int in question
 * Return - returns 1 if c is a digit, if else 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
